namespace CSharpDesignPatterns.Creational.FactoryMethod
{
    /// <summary>
    /// Define an interface for creating an object, but let
    /// subclasses decide which class to instantiate. This
    /// lets a class defer instantion to subclasses.
    /// 
    /// Allows programmers to request objects and have the 
    /// correct type created behind the scene and returned.
    /// </summary>
}
